import SimpleBar from 'simplebar-react';
import { styled } from '@mui/material/styles';

/**
 * A styled component that wraps SimpleBar, a scrollbar plugin for React.
 *
 * @returns {JSX.Element} The rendered SimpleBar component.
 */
export const Scrollbar = styled(SimpleBar)``;
